#include <stdio.h>
//size of operators
int main ( void ){
  printf("size of char is %d", sizeof (char));
   printf("size of char is %d", sizeof (short));
    printf("size of char is %d", sizeof (int));
     printf("size of char is %d", sizeof (long));
      printf("size of char is %d", sizeof (float));
       printf("size of char is %d", sizeof (double));
   

return 0;
}
//prints the prompt
int a;
float b;
int main(void ){
printf("enter the integer and the float:");
scanf("%d %f",&a,&b);
printf("the integer is %d and the float is %f and their sum is %f",a,b,a+b);
return 0;



}

